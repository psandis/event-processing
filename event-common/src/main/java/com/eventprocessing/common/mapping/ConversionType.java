package com.eventprocessing.common.mapping;

public enum ConversionType {
    NONE,
    TO_STRING,
    TO_INTEGER,
    TO_LONG,
    TO_DOUBLE,
    TO_BOOLEAN,
    TO_TIMESTAMP,
    TO_UPPER,
    TO_LOWER,
    FLATTEN,
    MASK
}
